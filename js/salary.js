/* Salary frontend shim
   Salary UI logic is owned by salary.html.
*/

(function () {
  "use strict";

  window.SovereignSalaryScript = {
    version: "shim-1.0.0",
    status: "loaded"
  };

  console.log("[salary shim] loaded");
})();
